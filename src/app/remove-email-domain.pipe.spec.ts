import { RemoveEmailDomainPipe } from './remove-email-domain.pipe';

describe('RemoveEmailDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveEmailDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
